language: java

install: mvn -q clean

# clean / compile - test - package - verify - install - deploy / site
script: mvn verify

jdk:
  - oraclejdk8
  - openjdk8
  - openjdk11

#branches:
#  only:
#    - master

notifications:
  email:
    - bernd-2018@eckenfels.net
